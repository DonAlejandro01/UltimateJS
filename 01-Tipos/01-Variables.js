/*Variable reservada */

let nombre = `Hola mundo `;
let apellido;

/*Cambiar contenido de variable */
apellido = 'Perez';

/*camelCase, UpperCamelCase y Snake_Case*/
let NombreCompleto;
let nombreCompleto;
let nombre_completo;


/*
REGLAS:

-Las variables no pueden ser nombras como palabras reservadas
-No se puede anteponer un numero
- UpperCamelCase: El inicio de cada palabra con mayusculas
- camelCase: la primera palabra no commo mayuscula,pero el resto si (mas ocupada)
- Snake_Case: Cada palabra separada por un guion bajo
- Javascript es case sensitive: sabe diferenciar entre nombres similares pero con diferencia de mayusculas
- Variables con nombre objetivos
*/
console.log(nombre);